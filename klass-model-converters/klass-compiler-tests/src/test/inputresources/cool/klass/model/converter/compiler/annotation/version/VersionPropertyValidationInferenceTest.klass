package com.version

user TestUser systemTemporal
{
    userId: String id key userId maxLength(128);
}

class VersionedEntity systemTemporal versioned audited
{
    entityId: Long id key minimum(1) maximum(9999999);
    name: String maxLength(200);
}
